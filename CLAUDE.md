# CLAUDE.md — hacking-terminal 프로젝트 규칙

이 문서는 Claude Code가 이 저장소에서 작업할 때 항상 따라야 하는 규칙입니다.

## 프로젝트 목적
- 인스타 릴스 콘텐츠용 데모: 그럴듯한 "해킹중" 터미널 화면을 자동 재생하는 웹 데모
- 실제 제품이 아니라 촬영/캡처용 소품. 완성도(비주얼 디테일)가 최우선, 백엔드/실서비스 요구사항 없음

## 절대 규칙 (No Self-Execution Policy) — 비타협적
- Claude(Code 포함)는 **코드 작성까지만** 담당한다.
- `npm run dev`, `npm run build`, `npm run preview` 등 **실행/빌드/미리보기는 절대 자율적으로 실행하지 않는다.**
- 반복 실행 사이클(수정 → 실행 → 확인 → 재수정)을 자체적으로 돌리지 않는다.
- 코드 수정 후에는 "어떤 파일을 왜 고쳤는지" + "깜자가 직접 확인할 방법(번호로 정리한 절차)"만 안내한다.
- 과거 CursorToy 프로젝트에서 불안정한 자동 실행 빌드로 모니터가 먹통 된 사고가 있었기 때문에 예외 없음.

## 기술 스택
- React + Vite (JavaScript, TypeScript 아님)
- 외부 상태관리 라이브러리 없이 `useState`/`useRef`/`useEffect`만 사용 (가벼운 데모이므로 과설계 금지)
- 스타일은 일반 CSS(`App.css`) 유지, CSS-in-JS나 Tailwind 등 새 의존성 추가 전에는 먼저 물어볼 것

## 콘텐츠/데이터 생성 규칙 (중요 — 안전 규칙)
- 로그에 등장하는 IP는 반드시 **사설 대역만** 사용: `10.x.x.x`, `192.168.x.x`, `172.16~31.x.x`
- 실제 존재하는 회사명, 서비스명, 도메인, 상표를 로그 문구에 절대 넣지 않는다 (전부 가짜 시스템 콜/모듈명으로 구성)
- 해시값, 포트, PID 등은 전부 랜덤 생성값만 사용 (진짜처럼 보이되 진짜 의미는 없어야 함)

## 코드 구조
- `src/App.jsx` : 핵심 로직 — 랜덤 로그 생성기(`LOG_GENERATORS`), 타이핑 애니메이션, 진행바, ACCESS GRANTED 글리치 시퀀스
- `src/App.css` : 비주얼 — CRT 스캔라인, 비네트, 터미널 색상 토큰
- 새 비주얼 이펙트를 추가할 때는 기존 색상 톤(초록 `#35ff8a` 계열) 및 `text-shadow` 글로우 패턴을 따라간다. 색상 팔레트를 통째로 바꾸는 변형 버전(앰버/사이버펑크 등)을 만들 땐 별도 브랜치나 별도 폴더로 분리해서 원본을 보존한다.

## 작업 방식
- 한 번에 하나의 개선 사항만 작업하고, 불안정해 보이면 바로 이전 상태로 롤백 제안
- Tauri/Rust 관련 내용은 이 프로젝트에는 해당 없음 (이 프로젝트는 순수 웹). 데스크톱 버전으로 전환이 필요해지면 별도로 논의 후 CursorToy의 오버레이/TrayIcon 패턴을 참고할 것